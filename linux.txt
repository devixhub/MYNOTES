shell 
#!/bin/bash
for i in {1..50}
do
        if((i % 3 == 0 && i % 5 == 0 && i % 9 != 0))
        then
                echo "$i"
        fi
done
-----------------------------------------------
Production Server Troubleshooting Flow
uptime->load cheacking
nproc ->cpu cores



-----------------------------------
find / -type f -mtime -15